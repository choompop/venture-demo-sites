import { ventures } from '../../data';
import { notFound } from 'next/navigation';
const basePath = process.env.GITHUB_PAGES === 'true' ? '/venture-demo-sites' : '';
export function generateStaticParams(){ return ventures.map(v=>({slug:v.slug})); }
export function generateMetadata({params}:{params:{slug:string}}){ const v=ventures.find(x=>x.slug===params.slug); return {title: v ? `${v.name} — Demo` : 'Demo'}; }
export default function VenturePage({params}:{params:{slug:string}}){ const v=ventures.find(x=>x.slug===params.slug); if(!v) return notFound(); return <><nav className="nav"><div className="navin"><a className="brand" href={basePath || '/'}>← Demo Lab</a><div className="navlinks"><a href="#site">Site</a><a href="#dashboard">Dashboard</a><a href="#ops">Ops</a></div></div></nav><main><section className="routeHero"><div className="wrap"><div className="eyebrow" style={{color:v.accent}}>{v.type} / {v.domain}</div><h1>{v.name}</h1><p className="lead">{v.line}</p><div className="path"><a className="button primary" style={{background:`linear-gradient(135deg, ${v.accent}, #8b5cf6)`}} href="#dashboard">View dashboard demo</a><a className="button" href={basePath || '/'}>All ventures</a></div></div></section><section id="site" className="section"><div className="wrap split"><div><h2 className="big">Public website frame</h2><p className="muted">A focused external surface for the brand: clear promise, audience, offer, trust signals, lead capture, and content lanes. This page is a demo direction, not final public copy.</p></div><div className="panel"><h2>Homepage sections</h2><div className="pillrow">{['Hero','Offer','Proof','Resources','Intake','Footer'].map(x=><span className="pill" key={x}>{x}</span>)}</div></div></div></section><section id="dashboard" className="section"><div className="wrap"><h2 className="big">Demo dashboard modules</h2><div className="modules">{v.modules.map(m=><div className="panel module" key={m}><h3>{m}</h3><p className="muted">Sample module for demo data, intern workflows, and future client operating systems.</p></div>)}</div></div></section><section id="ops" className="section"><div className="wrap split"><div className="panel"><div className="eyebrow">Guardrail</div><p className="muted">{v.caution}</p></div><div className="panel"><div className="eyebrow">Shared stack</div><p className="muted">Domain → Vercel → Google Drive → Slack → Hermes context → dashboard → approval workflow.</p></div></div></section></main><footer className="footer"><div className="wrap">{v.name} demo — part of the Kyburz venture operating system.</div></footer></>}
